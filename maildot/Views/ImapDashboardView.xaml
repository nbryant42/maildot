<UserControl
    x:Class="maildot.Views.ImapDashboardView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:maildot.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Padding="16" BorderBrush="{ThemeResource SurfaceStrokeColorDefaultBrush}" BorderThickness="1">
            <StackPanel>
                <TextBlock Text="Folders" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,12" />
                <ListView>
                    <ListViewItem Content="Inbox" />
                    <ListViewItem Content="Sent" />
                    <ListViewItem Content="Drafts" />
                    <ListViewItem Content="Archive" />
                    <ListViewItem Content="Spam" />
                </ListView>
            </StackPanel>
        </Border>

        <Grid Grid.Column="1" Padding="24">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <StackPanel>
                <TextBlock x:Name="AccountSummaryTextBlock" Text="Connected to IMAP" FontSize="18" FontWeight="SemiBold" />
                <Button x:Name="ReauthButton" Content="Re-enter password" Click="OnReauthClicked" HorizontalAlignment="Left" Margin="0,12,0,0" />
            </StackPanel>

            <Border Grid.Row="1" Margin="0,20,0,0" Padding="16" Background="{ThemeResource LayerFillColorDefaultBrush}" CornerRadius="8">
                <StackPanel>
                    <TextBlock Text="Mailbox preview" FontSize="16" FontWeight="SemiBold" />
                    <TextBlock Text="Select a folder to show messages here. Vector experiments will surface here as well." Margin="0,4,0,12" TextWrapping="Wrap" />
                    <ListView>
                        <ListViewItem Content="Sample message 1" />
                        <ListViewItem Content="Sample message 2" />
                    </ListView>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>
